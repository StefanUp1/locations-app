import React from "react";
import "./loader.scss";

// Here we could create "Loader" component to display while waiting for api call to finish, component to lazy load, etc...
const Loader = () => <div className="loader">Loading....</div>;

export default Loader;
